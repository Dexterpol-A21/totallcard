}
.nav-item:hover::after { height: 100%; transition-delay: 0.4s; }

/* ---- CARDS ANIMATION (DEAL EFFECT) ---- */
.deal-ready {
    opacity: 0;
    transform: translateY(120px) rotate3d(1, 1, 1, 15deg) scale(0.85);        
}
.deal-active {
    opacity: 1;
    transform: translateY(0) rotate3d(0, 0, 0, 0) scale(1);
}

/* Efecto Holograma/Reflejo Global para todas las Tarjetas */
.pvc-card::after {
    content: "";
    position: absolute;
    inset: 0;
    background: linear-gradient(90deg, rgba(255,255,255,0) 0%, rgba(255,255,255,0.3) 50%, rgba(255,255,255,0) 100%);
    transform: translateX(-150%);
    transition: transform 0.8s ease-in-out;
    pointer-events: none;
    z-index: 50;
    border-radius: inherit;
}
.pvc-card:hover::after {
    transform: translateX(150%);
}

/* From Uiverse.io by barisdogansutcu */
.download-button {
  position: relative;
  border-width: 0;
  color: white;
  font-size: 13px; /* Texto más grande */
  font-weight: 600;
  border-radius: 4px;
  z-index: 1;
}

.download-button .docs {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 0.5rem;
  min-height: 38px;
  padding: 0.5rem 1.25rem; /* Mucho menos padding horizontal y vertical */
  border-radius: 4px;
  z-index: 1;
  background-color: #e1061a;
  transition: all 0.5s cubic-bezier(0.77, 0, 0.175, 1);
}

.download {
  position: absolute;
  inset: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  max-width: 90%;
  margin: 0 auto;
  z-index: -1;
  border-radius: 4px;
  transform: translateY(0%);
  background-color: #25D366; /* Verde WhatsApp */
  border: solid 1px #25D366;
  transition: all 0.5s cubic-bezier(0.77, 0, 0.175, 1);
}

.download-button:hover .download {
  transform: translateY(100%);
}

.more-button {
  position: relative;
  border-width: 0;
  color: #E1061A;
  font-size: 13px;
  font-weight: 600;
  border-radius: 4px;
  z-index: 1;
}

.more-button .docs {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 0.5rem;
  min-height: 38px;
  padding: 0.5rem 1.25rem;
  border-radius: 4px;
  z-index: 1;
  background-color: white;
  border: 1px solid #E1061A;
  color: #E1061A;
  transition: all 0.5s cubic-bezier(0.77, 0, 0.175, 1);
}

.more-button .download {
  position: absolute;
  inset: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  max-width: 90%;
  margin: 0 auto;
  z-index: -1;
  border-radius: 4px;
  transform: translateY(0%);
  background-color: #E1061A;
  color: white;
  border: solid 1px #E1061A;
  transition: all 0.5s cubic-bezier(0.77, 0, 0.175, 1);
}

.more-button:hover .download {
  transform: translateY(100%);
}

/* Removemos la animación del ícono para que el avioncito se quede quieto */
.download svg {
  transition: transform 0.3s ease;
}

</style>
